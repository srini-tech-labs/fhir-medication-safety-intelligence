# Copy to .env (git-ignored) and fill in. Used ONLY by the opt-in live tooling (scripts/live_claude_check.py);
# the API server does not read this file. Values already exported in your shell take precedence.
ANTHROPIC_API_KEY=
# Optional: override the explanation model for live runs (default claude-opus-5).
# EXPLANATION_MODEL=claude-sonnet-5
